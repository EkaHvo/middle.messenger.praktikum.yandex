<input 
    class="input" 
    type={{ type }} 
    id={{ id }} 
    name={{ name }} 
    {{#if value}}
        value={{ value }}
    {{/if}}
    {{#if placeholder}}
        placeholder={{ placeholder }}
    {{/if}}
    {{#if readonly}}
        readonly={{ readonly }}
    {{/if}}>
